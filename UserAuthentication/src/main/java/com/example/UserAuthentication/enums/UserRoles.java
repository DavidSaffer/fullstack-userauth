package com.example.UserAuthentication.enums;

/**
 * Enum defining the possible roles for users within the system.
 *
 * Roles:
 * - {@code ADMIN}: Represents administrative users who have higher privileges.
 * - {@code USER}: Represents regular users with standard access rights.
 */
public enum UserRoles {
    ADMIN, USER;
}
